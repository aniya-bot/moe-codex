import CharactersClient from "@/app/characters/CharactersClient";

export const metadata = {
  title: "Characters · Moe Codex",
};

export default function CharactersPage() {
  return <CharactersClient />;
}
